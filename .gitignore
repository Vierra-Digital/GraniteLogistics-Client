# Dependencies
node_modules/
**/node_modules/

# Next.js app (kept local, not part of the deploy repo) + build output
next/
.next/

# Build / runtime artifacts
*.log
server/data/
server/data.json

# Env files (never commit secrets)
.env
# VAPID keypair written by `npm run vapid` — contains a private key
vapid-keys.local.txt
.env.local
**/.env.local

# Rendered screenshots from `npm run shots`
shots/

# Local tooling
.claude/launch.json

# OS / editor cruft
.DS_Store
Thumbs.db
